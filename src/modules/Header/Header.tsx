import React, { useState, useEffect } from "react";

const Header = (props: {}) => {
  useEffect(() => {
    // search api
    // https://www.woolworths.com.au/apis/ui/Search/suggestion?Key=ai
  });
  return <div>Header</div>;
};

export default Header;
